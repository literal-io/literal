#if( $ctx.error )
  $util.error($ctx.error.message, $ctx.error.type, null, $ctx.result.cancellationReasons)
#else
  #set( $ctx.stash.output.annotation = $ctx.stash.oldAnnotationResult )
  #if( !$util.isNull($ctx.stash.newBody) )
    #set( $ctx.stash.output.annotation.body = $ctx.stash.newBody )
  #end
  #if( !$util.isNull($ctx.stash.newTarget) )
    #set( $ctx.stash.output.annotation.target = $ctx.stash.newTarget )
  #end
#end

{}
