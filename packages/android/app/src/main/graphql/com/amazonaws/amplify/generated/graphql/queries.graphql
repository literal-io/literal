query GetAnnotationForNotification($creatorUsername: String!, $id: String!) {
  getAnnotation(creatorUsername: $creatorUsername, id: $id) {
    id
    target {
      ... on TextualTarget {
        value
      }
    }
  }
}